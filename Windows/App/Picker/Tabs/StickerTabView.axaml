<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:picker="clr-namespace:PHTV.Windows.Picker"
             x:Class="PHTV.Windows.Picker.Tabs.StickerTabView"
             x:DataType="picker:EmojiPickerViewModel">

  <DockPanel>
    <!-- Search bar -->
    <Border DockPanel.Dock="Top" Margin="12,8" Padding="8,6"
            CornerRadius="8" Background="#15FFFFFF">
      <Grid ColumnDefinitions="Auto,*,Auto">
        <TextBlock Grid.Column="0" Text="🔍" FontSize="13"
                   VerticalAlignment="Center" Margin="0,0,6,0"
                   Foreground="#70FFFFFF" />
        <TextBox Grid.Column="1"
                 Text="{Binding SearchText, Mode=TwoWay}"
                 Watermark="Tìm sticker..."
                 FontSize="13"
                 Background="Transparent"
                 BorderThickness="0"
                 Foreground="#CCFFFFFF"
                 CaretBrush="#CCFFFFFF"
                 Name="SearchBox" />
        <Button Grid.Column="2"
                Content="✕" FontSize="11"
                Background="Transparent"
                Foreground="#60FFFFFF"
                Padding="4,2"
                IsVisible="{Binding SearchText.Length}"
                Click="OnClearSearch"
                Cursor="Hand" />
      </Grid>
    </Border>

    <!-- Sticker grid -->
    <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                  VerticalScrollBarVisibility="Auto">
      <StackPanel Margin="12,4,12,12">
        <!-- Recent Stickers -->
        <StackPanel IsVisible="{Binding !SearchText.Length}">
          <TextBlock Text="Gần đây" FontSize="11" Foreground="#70FFFFFF"
                     Margin="0,0,0,4"
                     IsVisible="{Binding RecentStickers.Count}" />
          <WrapPanel Name="RecentStickersPanel" />
        </StackPanel>

        <!-- Trending / Search results -->
        <TextBlock Text="Stickers"
                   FontSize="11" Foreground="#70FFFFFF"
                   Margin="0,8,0,4"
                   IsVisible="{Binding StickerResults.Count}" />
        <WrapPanel Name="StickerGridPanel" />

        <!-- Loading -->
        <TextBlock Text="Đang tải..."
                   FontSize="12"
                   Foreground="#60FFFFFF"
                   HorizontalAlignment="Center"
                   Margin="0,20"
                   IsVisible="{Binding IsLoading}" />

        <!-- Empty state -->
        <TextBlock Text="Không tìm thấy sticker nào"
                   FontSize="12"
                   Foreground="#50FFFFFF"
                   HorizontalAlignment="Center"
                   Margin="0,20"
                   Name="EmptyText" IsVisible="False" />
      </StackPanel>
    </ScrollViewer>
  </DockPanel>
</UserControl>
